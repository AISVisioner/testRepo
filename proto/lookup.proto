syntax = "proto3";

package lookup;

service Lookup {
  rpc Match (MatchRequest) returns (MatchResponse) {}
}

message MatchRequest {
  repeated float point = 1 [packed=true];
}

message MatchResponse {
  optional string id = 1;
}